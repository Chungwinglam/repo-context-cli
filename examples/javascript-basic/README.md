# JavaScript Basic Example

This fixture shows the smallest repository shape that Repo Context CLI should handle well: a package file, one source file, and one test script.

Try it after building the CLI:

```bash
node ../../dist/cli.js pack --dry-run --for codex
```
