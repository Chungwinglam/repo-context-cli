# TypeScript Vite Example

This fixture represents a common TypeScript frontend repository shape. It is intentionally small so detection and generated output remain easy to inspect.

Try it after building the CLI:

```bash
node ../../dist/cli.js pack --dry-run --for cursor
```
